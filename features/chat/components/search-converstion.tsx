import React from 'react';
import { Text, View } from 'react-native';

type Props = {};

export const SearchConversation = (props: Props) => {
  return (
    <View>
      <Text>SearchCoversation</Text>
    </View>
  );
};
